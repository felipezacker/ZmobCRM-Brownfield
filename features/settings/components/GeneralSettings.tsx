'use client'

import React from 'react'

/**
 * @deprecated Use CompanySettings instead. This component is kept for backward compatibility
 * but the /settings route now redirects to /settings/empresa.
 */
export function GeneralSettings() {
  return null
}

export default GeneralSettings
